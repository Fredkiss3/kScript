cmp Hello do
	# Each file should have a "cmp" (component) definition
	# where the attributes are defined
endcmp

# If a program has a "main" function, it runs it
func main(String[] args) do
	print("Hello world")
endfun
